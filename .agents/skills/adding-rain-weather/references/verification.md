# 天气专项验证

按当前任务读取命中段落，各专项只在对应机制受影响时适用；流程与审批遵循 [技能入口](../SKILL.md) 和 [AGENTS.md](../../../../AGENTS.md)。

## 验证清单

- `set_weather CLEAR/LIGHT/MEDIUM/HEAVY` 固定最终档位；该测试入口会立即完成过渡，适合精确数值断言。
- 需要验证自然两秒过渡时，用短天气倒计时触发正式切档，再断言 `previousIntensity`、`transitionOn` 和结束状态。
- 迷雾快照往返要在 `timeScale=0` 下于重载命令后立即断言逐格 alpha、可见格数和截图，不能先等一帧掩盖首帧透明问题。
- 地图专属积累器至少覆盖自然满值后的目标数与残留值范围、固定多目标正反行、活动阶段快照往返、结束后兑现并清空 pending，以及不支持地图 no-op；世界特效必须读同步截图，防止逻辑通过但锁定行或坡面采样错位。
- 出生变异先固定天气，再走专用生产入口或新增一个只调用正式解析器的 AutoTest 命令；不要用通用 `spawn_zombie` 冒充波次随机生成。
- 实体 dump 至少暴露实际类型、变异标志、技能计时/次数或可精确断言的整数投影。
- 有时限整栏干扰专项要从无公开预报开始，断言来源仍主动提交窗口、单次时长和暂停/倍速语义；若同类可叠加，再断言第二个来源能在活动窗口内起手、贡献按剩余时长相加、快照保留叠加值，并跨过第一份贡献原本的到期点后仍生效。存在同时余时上限时还要导出并断言其口径。随后在窗口内依次公开雨雪/台风、寒潮和雾势，断言各代际被当帧截获、锁定真实值未变，栏目只剩统一故障文案且独立场地仪表保留。还要在窗口前让专用中央预警实际出现，断言提交只撤下天气类提示，并覆盖窗口内不再新弹预警/失败。最后验证到期后的实况恢复，以及已截获且尚未揭晓的预报不会因窗口结束提前泄露。
- 按本次涉及的状态选择验证：天气边沿看晴天/目标雨势/放晴；随机或持久化看读档不重 roll；阵营、控制或时钟改动分别看魅惑、减速/冻结、暂停与倍速。
- 实体改天专项额外覆盖：不支持地图 no-op、弱天气升档、已有更强天气不降级、周期同档不刷新、一次性同档补时、是否生成台风、触发节奏与 Board 天气同时快照往返。
- 台风偏好专项覆盖：总开关默认开启、关闭后概率为 0、强制入口拒绝且 pending/active 归一；所有非冬日花园无尽地形允许台风，雪原仍按背景拒绝。开局保护另覆盖条件显示、隐藏不改写已保存选择、普通冒险和无尽首轮的第 1～5 波、首个放行波、保护性无台风不累计 pity，以及预警期 pending 状态快照往返。控制台 UI 同时覆盖整行悬停/点击、Tooltip 实文、随鼠标移动、移出消失和条件项隐藏后无残留说明。
- 验证矩阵按实际改动面选择：天气逻辑、概率、出怪或普通资源修改只跑默认 `clang-release` 可见专项；不因新天气粒子或新内容本身加跑 `-NoInstance`/OpenGL。只有实际改到渲染后端、后端兼容路径或跨后端提交实现时，才补默认 Vulkan + `-NoInstance` + 强制 OpenGL 矩阵。
- 视觉或 UI 改动检查截图；同时检查退出码、`run.log` 的 `script finished OK`、状态 JSON 和断言。
- 固定复合天气至少覆盖生效前一波的预报、生效波的锁定组合/倍率/倒计时、一次性资源读档不返还、常态黑幕与闪光两张截图，以及 UI 贴图未被世界覆盖层遮挡。
- 降水表现派生至少覆盖阈值两侧的同一雨势、旧特效被替换、玩法雨势/倍率不变、禁用的音画副作用、地图台风全入口 no-op，以及并行环境状态的快照往返；纹理覆盖与降水粒子必须读实际截图，不能只断言效果名。
