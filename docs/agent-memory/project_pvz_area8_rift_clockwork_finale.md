# 第八大关极光祭司、极夜钟匠与终盘植物

## 当前状态

2026-09-11 调整入场强度：极光祭司在逻辑 X 超出 Board 最右列右缘且行走时，原有速度再乘 `kOutsideWalkMultiplier=2.0`（含过载）；步频与根运动同步加快，入场后恢复原倍率，啃食、前摇和死亡不加速。仍须入场才开始祭司前摇。极夜钟匠移除前摇的入场位置限制，场外准备/冷却到期即可施法，原有 2 秒准备、3.2 秒前摇、10 秒循环保持不变。倍率从当前位置派生，无新增存档字段。按主人要求不运行 AutoTest；已有入场脚本同步修改钟匠的过时预期，不作为本轮运行证据。

本轮 `clang-release` 构建 exit 0，378 项 Win7 导入审计通过；vcpkg applocal 仍提示缺少 DLL 扫描工具。静态差异与脚本 JSON 检查通过；僵尸技能及相关 references 已按改动范围审计，补入共用边界与派生倍率规则，`quick_validate.py` 通过。本轮未修改本体、附件、reanim、粒子或绘图入口，未新增视觉占位符，也未进行运行截图验收。

2026-08-29 已按 `docs/superpowers/specs/2026-08-29-area8-rift-clockwork-finale-design.md` 完成 8-7、8-8。默认 Vulkan 的可见 `clang-release` 专项覆盖资源、数值、仪式阶段、独立事务、界碑改道、时间锚恢复、三仪表曙光结算和三组中途存档；最终状态无粒子资源警告。主体动画全部复用经典时间轴，没有新增帧事件：两种僵尸复用普通僵尸，界碑花复用金盏花，曙光莲复用睡莲，身份由命名 follower、确定性贴图和复合粒子建立。

## 8-7 极光祭司与界碑花

`ZOMBIE_AURORA_PRIEST` 为 1200 本体、800 非磁性仪器、0.65 倍速度和 50 啃食；仪器破坏后永久取消未提交仪式并过载到 1.15 倍速度、150 啃食。首次出生准备 6 秒后进入 2.8 秒可中断前摇；每次提交边沿立即开始 5 秒普通循环冷却，再进入完整前摇。冻结、黄油和麻痹暂停，减速按 0.5 倍推进，警铃草打断后等待 5 秒重试。提交后由 Board 独立保存 0.8 秒裂隙事务，常态 3 个、白毛风 4 个，目标在第 3～7 列，至少两行、每行最多两个，优先有植物格并排除雪穴；各生成一只铁桶、铁门、扶梯、跳跳或橄榄球。施法者死亡、掉头、魅惑或破甲只取消未提交阶段，不回滚裂隙。

`PLANT_BOUNDARYFLOWER` 为 200 阳光、30 秒冷却、450 生命、普通层；每 15 游戏秒获得一枚碎片，最多 2 枚，停机暂停充能与保护。覆盖完整 3×3；每个裂隙、雪穴出生或时间回位事务在真正提交位置前，由 Board 按距离和稳定植物 ID 选唯一提供者并消费一枚。拒绝出生时改到该行右侧；拒绝存活目标回位时留在当前位置但仍恢复数值，拒绝死亡目标复活时在该行右侧恢复。

8-7 为 30 波、900 初始阳光；第 3 波保底，第 4 波起自然出现。冒险每波最多 3 只且同时最多 3 只；权重/成本 4000，生存第 20 轮出现。通关奖励界碑花。

## 8-8 极夜钟匠与曙光莲

`ZOMBIE_POLAR_CLOCKMAKER` 为 1000 本体、1200 非磁性星盘、0.7 倍速度和 50 啃食。首次出生准备 2 秒后进入 3.2 秒可中断前摇；每次提交边沿立即开始 10 秒普通循环冷却，再进入完整前摇。控制与减速规则同祭司，警铃草打断后等待 4 秒。提交时按威胁度记录同/邻行最多 12 个敌对目标，排除首领与已被其他锚记录的目标，并形成与施法者生命周期解耦的 6 秒时间锚。

快照保存稳定 ID、类型、行/X、本体与两类防具、头手、控制余时，并通过 `ZombieTemporalAbilityState` 窄接口精确保存祭司与钟匠的准备、重试、前摇、普通冷却或禁用阶段及剩余时间；适应头盔另记录 `NONE/PLANT_LINEAGE/ASH` 完整适应来源，并与恢复后的头盔和胸章原子对齐。目标存活时恢复核心数值并尝试回位；死亡时用原稳定 ID 复建同类型。除这三种外的其他品种没有局部能力快照：存活者保留当前内部状态，死亡复建者从该类型初始安全状态开始，因此不承诺恢复投篮车篮球、巨人小鬼或雪橇编队。魅惑、小推车和大嘴花吞食为不可逆，磁力菇抽走的装备不恢复。若祭司或另一名钟匠在快照时仍处于准备、重试、前摇或普通冷却，即使锚定期间已经提交，结算仍回到原阶段与原余时；已落地裂隙或时间锚等 Board 事务不回滚、不重放。来源钟匠不记录也不恢复自己的局部阶段，避免六秒结算刷新冷却；另一名钟匠可以精确恢复它。奖杯沿用当前实体结算，出现奖杯时清空全部未兑现裂隙和时间锚。8-8 第 2 波保底、第 3 波起自然出现；冒险每波最多 2 只且同时最多 2 只，权重/成本 4500，生存第 22 轮出现。

2026-08-30 修复存活目标断头/断臂后的表现回溯：`RestoreTemporalCoreState` 仍在原对象恢复 `mHasHead/mHasArm`，随后由 `OnTemporalCoreStateRestored` 静默撤销 `HeadDrop/ArmDrop` 留在 Animator 上的隐藏与残肢换图，不重放掉落物、粒子或声音。普通骨架由基类重建；8-8 自然池与祭司裂隙涉及的橄榄球、扶梯/精英扶梯、潜雪、铁门和跳跳各自恢复专属轨道。存活目标不采用强制 `CreateZombieWithID`，避免啃食、附件、碰撞与 Board 事务仍引用旧对象。可见 `clang-release` 专项在中途存档往返后锁定同一普通僵尸的 `hasHead/hasArm/headVisible/armVisible` 从全 false 恢复为全 true。

2026-08-30 修复存活目标在啮食中回溯后出现 `mIsEating=true` 但轨道永久停在 `anim_walk/anim_walk2` 的组合状态。根因是钟匠/祭司局部能力阶段恢复会选择走路轨道，但不拥有啮食关系；主循环随后因 `mIsEating` 早退，碰撞也没有新的开吃边沿可重播动画。Board 现在于核心数值和全部局部阶段恢复完成后调用 `Zombie::ReconcileTemporalEatingPresentation`：有效植物/僵尸关系重播该品种 `anim_eat` 并重建 `OnStartEating` 表现，已因位置回溯而失效的植物关系则走既有原子收尾以平衡 `mEaterCount`。可见 `clang-release` 专项用另一名钟匠记录正在啮食南瓜的钟匠，锁定结算前后均为 `isEating=true/anim_eat`且南瓜 `eaterCount=1`。

同日进一步收紧 `mIsDying` 倒地壳的稳定 ID 复建：`Board::ReplaceDyingZombieWithID` 先构造不登记、不增加 `mZombieNumber` 的替身，旧壳在注册表仍指向自身时通过 `Zombie::RetireForTemporalReplacement` 走派生清理并静默失活，随后同一 ID 与原有计数所有权直接交给替身。旧壳不会发死亡光环、雾火奖励、扣计数或检查胜利，因此不存在新旧对象 `+1/-1` 的双计数窗口；普通目标专项在 `anim_death` 前后连续锁定 `zombieNumber==2`。`ZOMBIE_BOBSLED_TEAM` 属于一次候选四实体编队，现与首领一样从新锚候选和旧档锚结算中排除，专项确认四名队员存在时锚目标只有来源钟匠，整队与 `zombieNumber==5` 均保持不变。

`PLANT_DAWNLOTUS` 为 250 阳光、50 秒冷却、500 生命、普通层且场上同时最多一株。温度、湿度和风速每个红色仪表每秒各提供 1 能量，满 60 后由玩家点击释放；停机暂停充能并禁止释放。释放时只结算当前仍为红色的模块：温度对每行最高威胁目标造成 1200 点带曙光莲来源的植物伤害；湿度封闭全部雪穴并强制合法地下目标无伤出土；风速为 Board 提供 8 秒导航，忽略白毛风索敌距离和投掷物强风偏移。通关 8-8 奖励曙光莲。

## 表现、存档与验证契约

裂隙、时间锚和导航均由 Board 保存；僵尸保存本地动作阶段，时间锚另复制窄能力快照，植物保存碎片/充能。存档 schema 为 12；v10 活动锚迁移为无能力快照并继续使用旧 submitted 语义，v11 永久 `COMMITTED` 的祭司/钟匠实体及有效能力快照迁移为完整 5/10 秒普通冷却。祭司前摇使用青紫极光旋涡与模仿者 puff，钟匠则使用独立透明钟面、时针、分针三层：时间锚六秒内周期续显，两针按不同负 `ParticleSpinSpeed` 持续逆转，恢复边沿高速倒转并随钟面收缩，视觉语言不再与裂隙共用。裂隙与曙光释放仍组合自定义核心、模仿者 puff 云团和星屑，透明度与半径限定在目标附近。多 Emitter 的首个名称必须保持正式 `EmitEffect` 键；当前引擎无 `Attractor` Field，向内旋转使用负 `Away X` 叠 `Circle X`。

2026-09-05 解除祭司/钟匠能力的极夜地图特判：提交与独立事务读档可在其他地图执行，天气资格仍独立。矿场裂隙排除岩壁/孤立地面；时间锚保存矿道行基线偏移与目标格，回位或复活同步恢复，旧档默认0偏移/-1节点。既有能力周期、召唤池和常规出怪表不变。

2026-09-05 极光祭司增加每只累计最多3次释放：只有正式提交至少一道裂隙才增加 `ritualReleaseCount`，前摇被打断不计次数，第三次后进入 DISABLED 但保留正常移动、啃食与仪器表现。次数写入实体存档，并通过 `ZombieTemporalAbilityState.releaseCount` 与 Board 的 `abilityReleaseCount` 存入钟匠记录；按主人明确要求，回溯存活或死亡目标均恢复记录时次数，允许返还额度，独立裂隙仍不回滚。旧档缺字段默认0，新字段可用中性默认值兼容，无需提升 schema。专项为 `smoke_aurora_priest_release_limit.json`，覆盖中断、三次封顶、耗尽往返和钟匠存活/死亡回溯。

本次 `clang-release` 构建零警告；默认 Vulkan 当前桌面可见 `smoke_aurora_priest_release_limit`（73条命令）与 `smoke_area8_finale_contract` 均 exit 0/status passed，已检查实体/时间锚快照中的次数、run.log 与全部同步截图。僵尸技能及相关契约审计完成，补入正式释放计次和时间锚额度语义，`quick_validate.py` 通过；本次没有新增视觉资源或绘图入口。

2026-09-05 主人要求两者入场后再发动技能：`BeginWindup` 现在用 Board 最右列中心加半格宽作为入场边界（九列地图为第九列右缘），逻辑 X 尚在边界外时继续走路，不进入停步前摇。准备和冷却仍正常计时，到期后等入场；未增加存档字段或改变既有次数、前摇和冷却。专项 `smoke_finale_entry_gate` 覆盖边界外到期、快照往返、边界内正常施法及自然行走入场；原释放次数专项把场外静止钟匠移至场内。

本次可见 clang-release 的入场专项（52条命令）及释放次数回归（73条命令）均 exit 0/status passed，已核对 run.log、状态和全部截图。构建成功且 Win7 导入检查通过，但 vcpkg applocal 后处理提示未找到 DLL 扫描工具；实际游戏启动与上述测试通过。本次仅调整前摇资格，无新增视觉资源或绘图入口；相关僵尸技能审计及 quick_validate.py 通过。
